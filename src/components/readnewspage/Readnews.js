import React from "react";

const Readnews = ({ text, title }) => {
     return (
          <div>
               <div>{title}</div>
               <div>{text}</div>
          </div>
     );
};

export default Readnews;
